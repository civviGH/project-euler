#!/usr/bin/env python3

from aux import *

primes = generatePrimes(10, 100000)

def generateList(n, s):
    1

def numberOfPrimes(l):
    number = 0
    for p in l:
        if checkPrime(p):
            number += 1
    return number

for p in primes:
    for i in range(1, len(str(p))):
